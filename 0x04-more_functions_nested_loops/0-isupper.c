#include "main.h"

/**
 * _isupper - prints the last digit
 * @c: input number as integer
 * Return: last digit
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
